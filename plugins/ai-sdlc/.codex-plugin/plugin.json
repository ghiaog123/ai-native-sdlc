{
  "name": "ai-sdlc",
  "version": "0.1.0",
  "description": "The six-stage AI-native software development lifecycle from Anthropic's playbook, as an executable Codex plugin. Each stage produces one version-controlled Markdown artifact and stops at a human gate: Plan writes intent.md, Design turns an accepted intent into spec.md under org policy, Build turns an accepted spec into plan.md then implements it, Test proves the change and regression-tests the agent configuration itself, Deploy runs the REVIEW.md passes on the PR and stops for human approval, Maintain watches bands.yaml control bands and files the next intent.md when a band breaks. Ships eight skills, two read-only subagents, artifact-gate and prod-guard hooks, and templates for intent.md, spec.md, plan.md, project memory (written to AGENTS.md for Codex, CLAUDE.md for Claude Code, from one template so the two cannot drift), REVIEW.md and bands.yaml. Every hook is inert unless the repository contains a .sdlc/ directory.",
  "author": {
    "name": "Hieuvu7"
  },
  "license": "MIT",
  "homepage": "https://github.com/hieuvu7/ai-native-sdlc",
  "repository": "https://github.com/hieuvu7/ai-native-sdlc",
  "keywords": [
    "sdlc",
    "ai-native",
    "intent-driven",
    "spec-driven",
    "governance",
    "approval-gates",
    "control-bands",
    "code-review"
  ],
  "skills": "./skills/",
  "subagents": "./agents/",
  "hooks": "./hooks/hooks.codex.json",
  "interface": {
    "displayName": "AI-Native SDLC",
    "shortDescription": "Six-stage lifecycle with a human gate at every handoff",
    "longDescription": "Runs software delivery as a loop of committed Markdown artifacts instead of role handoffs: intent.md, spec.md, plan.md, a reviewed PR, then control-band monitoring that files the next intent.md. Skills drive each stage, subagents run the review passes and diagnose control-band breaches, and hooks enforce the gates at the tool layer -- no source write for a change without an accepted plan.md, and no production command without explicit human authorization. Installing this plugin cannot change behavior in a repository that has not opted in: every hook exits immediately unless a .sdlc/ directory exists.",
    "developerName": "Hieuvu7",
    "category": "Productivity",
    "capabilities": [
      "Read",
      "Interactive"
    ],
    "websiteURL": "https://github.com/hieuvu7/ai-native-sdlc",
    "defaultPrompt": [
      "Initialize the AI-native SDLC loop in this repository",
      "Where does each in-flight change sit in the SDLC loop?",
      "Customers cannot see their claim status; start the loop at Plan"
    ]
  }
}
